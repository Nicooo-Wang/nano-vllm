# 第 1 课（nano-vllm 课程）实现计划

> **给 agentic worker：** 必需子技能：用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 按任务逐个实现本计划。步骤用复选框（`- [ ]`）跟踪。

**目标：** 构建第 1 课的可运行内容（`TUTORIAL.md` + 学生版 `lab.py` + `solution/`），用 trace/observe/explain 的方式讲清 request 生命周期，并内置可运行的验证。

**架构：** `lab.py` 用 monkeypatch 替换 `Scheduler.add`/`Scheduler.postprocess`（**不动源码**）来捕获每步 `_trace` 和按 id 索引的每个 `Sequence`，在两条 prompt 上跑 nano-vllm，再用 `run_checks()` 断言 trace 与 request 生命周期的不变式一致，打印 `All checks passed ✓`。学生填两个 TODO（trace 记录 + `summarize_request`）；参考答案与"合成 trace 单测"放在 `solution/`。验证逻辑（`run_checks` 等）的 import 不依赖 nanovllm/torch，因此可用合成数据做 **GPU-free 单测**；真正的端到端运行（在 H200 上跑出 `All checks passed ✓`）是最后一个、且依赖环境的任务。

**技术栈：** Python 3.10+、nano-vllm（torch/triton/flash-attn/transformers）、uv、仅标准库的单测（不引入 pytest 依赖）。

## 关于 GPU / H200 的说明（重要，先读）

- **第 1 课的 lab 本身必须在 H200（或任意 CUDA 卡）上跑。** 学生执行 `uv run python lab.py` 时，nano-vllm 会加载 Qwen3-0.6B、在 GPU 上 prefill/decode，trace 才会被捕获、检查才会通过。这是 **Task 8，不可省略、不能用合成数据替代**。
- 本计划里凡是"GPU-free"，**仅指 Task 1–4 的合成 trace 单测**：它只用一个手写的假 `_trace` 测"检查代码本身"（`run_checks`/`summarize_request`/`traced_postprocess` 的记录格式）是否正确，**不跑 nano-vllm、不要 GPU**。
- **为什么这么设计**：本服务器 base 环境目前缺 torch/flash-attn/模型（见 `course/README.md` 前置依赖）。把"检查逻辑"做成可独立单测，工程师就能在**没有 GPU 的机器上**先把检查代码写对、验证逻辑无误，而不必等环境装好。真正的 `All checks passed ✓` 由 Task 8 在 H200 上兑现。
- **两层都不可少**：合成单测证明**检查代码逻辑**对；H200 运行证明**真实 trace**与假设一致。

## 全局约束

- **不要修改任何 `nanovllm/` 源码** —— 只在 `lab.py` 里用 monkeypatch 观察。
- **`lab.py` 和 `solution/lab_solved.py` 的 import 不能依赖 nanovllm/torch** —— 所有 nanovllm/transformers 的 import 都放在 `main()` 内部。这是合成单测能 GPU-free 的前提。
- **学生可见目录只放 `TUTORIAL.md` + `lab.py`**；其余（工程师/审核用）都在 `solution/` 下。
- 运行配置：`tensor_parallel_size=1`、`enforce_eager=True`、模型在 `~/huggingface/Qwen3-0.6B/`、`max_tokens=64`（缩短 lab 运行）。
- 合成单测用 **`python`（不是 `uv run`）** 跑，避免触发重量级 `uv sync`；端到端运行才用 `uv run`。
- 每个 task 一个 `git commit`。分支：`course/lesson1`（已建）。

## 文件结构

- `course/lessons/lesson1/lab.py` — **学生版** lab：模块全局（`_trace`/`_seqs`/`_orig_*`）、`traced_add`（提供）、`traced_postprocess`（TODO）、`summarize_request`（TODO）、`run_checks`（提供）、`main`/`_check_env`/`_print_trace`（提供）。
- `course/lessons/lesson1/TUTORIAL.md` — 讲义 + §0 环境初始化。
- `course/lessons/lesson1/solution/lab_solved.py` — 参考答案：`lab.py` 的两个 TODO 已填（用于自证 lab 可通过 + 当答案）。
- `course/lessons/lesson1/solution/test_checks.py` — 合成 trace 的标准库单测，测 `traced_*`/`summarize_request`/`run_checks`（无需 GPU）。
- `course/lessons/lesson1/solution/ANSWERS.md` — Task 2 解释题答案 + 期望 trace 形态 + TODO→答案映射。

---

## Task 1：搭 `lab_solved.py` 骨架 + 测试 harness + TDD `traced_add`

**文件：**
- 创建：`course/lessons/lesson1/solution/lab_solved.py`
- 创建：`course/lessons/lesson1/solution/test_checks.py`
- 测试：`course/lessons/lesson1/solution/test_checks.py`

**接口：**
- 产出：模块全局 `_trace: list`、`_seqs: dict`、`_orig_postprocess`、`_orig_add`；函数 `traced_add(self, seq) -> Any`（调用 `_orig_add` 并把 `seq` 按 `seq.seq_id` 存进 `_seqs`）。`traced_postprocess`、`summarize_request`、`run_checks`、`main` 的桩抛 `NotImplementedError`。

- [ ] **步骤 1：写 `lab_solved.py` 骨架**

```python
"""Lesson 1 lab (reference solution) — trace the journey of a request.

The student file `../lab.py` is this file with the two TODOs blanked.
Importing this module does NOT import nanovllm/torch (those live in main()),
so the synthetic tests in test_checks.py run without a GPU.
"""
import os

_trace = []            # one record per engine step
_seqs = {}             # seq_id -> Sequence
_orig_postprocess = None
_orig_add = None


def traced_add(self, seq):
    """Hook for Scheduler.add: remember every Sequence by id (provided)."""
    _seqs[seq.seq_id] = seq
    return _orig_add(self, seq)


def traced_postprocess(self, seqs, token_ids, is_prefill):
    raise NotImplementedError("Task 2")


def summarize_request(seq):
    raise NotImplementedError("Task 3")


def run_checks(max_tokens):
    raise NotImplementedError("Task 4")


def main():
    raise NotImplementedError("Task 4b")


if __name__ == "__main__":
    main()
```

- [ ] **步骤 2：写 `test_checks.py` harness + `traced_add` 测试（应通过）**

```python
"""Synthetic-trace unit tests for lab_solved (no GPU).
Run: python solution/test_checks.py
"""
import sys, os, types
sys.path.insert(0, os.path.dirname(__file__))
import lab_solved as lab


def test_traced_add_captures_and_calls_original():
    lab._seqs = {}
    seq = types.SimpleNamespace(seq_id=99)
    lab._orig_add = lambda self, s: "added"
    out = lab.traced_add("SELF", seq)
    assert out == "added"
    assert lab._seqs[99] is seq


TESTS = [v for k, v in sorted(globals().items()) if k.startswith("test_")]

if __name__ == "__main__":
    failures = 0
    for t in TESTS:
        try:
            t(); print(f"[PASS] {t.__name__}")
        except AssertionError as e:
            failures += 1; print(f"[FAIL] {t.__name__}: {e}")
    print(f"\n{len(TESTS)-failures}/{len(TESTS)} tests passed")
    sys.exit(1 if failures else 0)
```

- [ ] **步骤 3：运行测试，确认通过**（骨架已实现 `traced_add`）

运行：`python course/lessons/lesson1/solution/test_checks.py`
预期：先打印 `[PASS] test_traced_add_captures_and_calls_original`，再 `1/1 tests passed`，退出码 0。

- [ ] **步骤 4：提交**

```bash
git add course/lessons/lesson1/solution/lab_solved.py course/lessons/lesson1/solution/test_checks.py
git commit -m "feat(course/lesson1): 搭 lab_solved 骨架 + traced_add + 测试 harness"
```

---

## Task 2：TDD `traced_postprocess`（lab Task 1 —— trace 记录）

**文件：**
- 修改：`course/lessons/lesson1/solution/lab_solved.py`（替换 `traced_postprocess` 桩）
- 测试：`course/lessons/lesson1/solution/test_checks.py`（追加测试）

**接口：**
- 消费：`_orig_postprocess`（在 `main()` 里设置；测试里设成 fake），seq 对象需暴露 `.seq_id`、`.num_scheduled_tokens`、`.status.name`、`.is_finished`。
- 产出：往 `_trace` 追加记录，形状为 `{"is_prefill": bool, "before": [(seq_id, num_scheduled_tokens, status_name)], "token_ids": list[int], "after": [(seq_id, status_name, is_finished)]}`。`num_scheduled_tokens` 必须在调用原函数**之前**捕获（postprocess 内部在 `scheduler.py:85` 把它清零）。

- [ ] **步骤 1：写失败测试（追加到 `test_checks.py` 的 `TESTS =` 行之前）**

```python
def test_traced_postprocess_records_before_calls_then_after():
    called = []
    seqs = [types.SimpleNamespace(
        seq_id=7, num_scheduled_tokens=3,
        status=types.SimpleNamespace(name="RUNNING"), is_finished=False)]
    lab._trace = []

    def fake_orig(self, seqs, token_ids, is_prefill):
        called.append((self, is_prefill))
        seqs[0].status.name = "FINISHED"
        seqs[0].is_finished = True

    lab._orig_postprocess = fake_orig
    lab.traced_postprocess("SELF", seqs, [42], True)

    assert called == [("SELF", True)], "must call original exactly once"
    assert len(lab._trace) == 1
    rec = lab._trace[0]
    assert rec["is_prefill"] is True
    assert rec["before"] == [(7, 3, "RUNNING")], "capture nst BEFORE original"
    assert rec["token_ids"] == [42]
    assert rec["after"] == [(7, "FINISHED", True)], "capture status AFTER original"
```

- [ ] **步骤 2：运行测试，确认失败**

运行：`python course/lessons/lesson1/solution/test_checks.py`
预期：`[FAIL] test_traced_postprocess_records_before_calls_then_after: ...`（NotImplementedError），`1/2 tests passed`。

- [ ] **步骤 3：实现 `traced_postprocess`**（替换桩体）

```python
def traced_postprocess(self, seqs, token_ids, is_prefill):
    """TODO(student): record this step into _trace, then call the original.

    Each seq still carries its num_scheduled_tokens at entry (postprocess
    resets it to 0 internally at scheduler.py:85), so capture it in `before`.
    """
    before = [(s.seq_id, s.num_scheduled_tokens, s.status.name) for s in seqs]
    result = _orig_postprocess(self, seqs, token_ids, is_prefill)
    after = [(s.seq_id, s.status.name, s.is_finished) for s in seqs]
    _trace.append({
        "is_prefill": is_prefill,
        "before": before,
        "token_ids": list(token_ids),
        "after": after,
    })
    return result
```

- [ ] **步骤 4：运行测试，确认两个都过**

运行：`python course/lessons/lesson1/solution/test_checks.py`
预期：`2/2 tests passed`，退出码 0。

- [ ] **步骤 5：提交**

```bash
git add course/lessons/lesson1/solution/lab_solved.py course/lessons/lesson1/solution/test_checks.py
git commit -m "feat(course/lesson1): 实现 traced_postprocess 的 trace 记录"
```

---

## Task 3：TDD `summarize_request`（lab Task 3 —— 小 wrapper）

**文件：**
- 修改：`course/lessons/lesson1/solution/lab_solved.py`（替换 `summarize_request` 桩）
- 测试：`course/lessons/lesson1/solution/test_checks.py`（追加测试 + 场景 helper）

**接口：**
- 消费：`_trace`（Task 2 产出的记录形状）、一个 seq（暴露 `.seq_id`、`.num_prompt_tokens`、`.num_completion_tokens`）。
- 产出：`summarize_request(seq) -> (num_prompt_tokens: int, num_completion_tokens: int, total_steps: int)`，其中 `total_steps` = `_trace` 里 `before` 含 `seq.seq_id` 的记录数。

- [ ] **步骤 1：追加场景 helper + 失败测试（在 `TESTS =` 行之前）**

```python
def _seq(seq_id, prompt, completion, finished=False):
    s = types.SimpleNamespace(
        seq_id=seq_id,
        num_prompt_tokens=prompt,
        num_tokens=prompt + completion,
        token_ids=list(range(prompt + completion)),
        status=types.SimpleNamespace(name="FINISHED" if finished else "RUNNING"),
        is_finished=finished,
    )
    s.completion_token_ids = s.token_ids[s.num_prompt_tokens:]
    s.num_completion_tokens = s.num_tokens - s.num_prompt_tokens
    return s


def _rec(is_prefill, entries, token_ids):
    """entries: list of (seq_id, num_scheduled_tokens)."""
    return {
        "is_prefill": is_prefill,
        "before": [(sid, nst, "RUNNING") for sid, nst in entries],
        "token_ids": list(token_ids),
        "after": [(sid, "RUNNING", False) for sid, _ in entries],
    }


def _mark_finished(record, seq_id):
    record["after"] = [(sid, "FINISHED" if sid == seq_id else nm,
                        sid == seq_id or fin)
                       for (sid, nm, fin) in record["after"]]


def _build_valid_scenario():
    """seq0 在 4 个 completion token 时命中 eos；seq1 在 5 个时到 max。"""
    s0 = _seq(0, prompt=10, completion=4, finished=True)
    s1 = _seq(1, prompt=12, completion=5, finished=True)
    lab._seqs = {0: s0, 1: s1}
    lab._trace = [
        _rec(True, [(0, 10), (1, 12)], [100, 200]),   # step 0 prefill 两条
        _rec(False, [(0, 1), (1, 1)], [101, 201]),    # step 1 decode 两条
        _rec(False, [(0, 1), (1, 1)], [102, 202]),    # step 2 decode 两条
        _rec(False, [(0, 1), (1, 1)], [103, 203]),    # step 3 decode 两条，seq0 结束
        _rec(False, [(1, 1)], [204]),                 # step 4 只 decode seq1，结束
    ]
    _mark_finished(lab._trace[3], 0)
    _mark_finished(lab._trace[4], 1)
    return s0, s1


def test_summarize_request_counts_steps():
    s0, s1 = _build_valid_scenario()
    assert lab.summarize_request(s0) == (10, 4, 4)
    assert lab.summarize_request(s1) == (12, 5, 5)
```

- [ ] **步骤 2：运行测试，确认失败**

运行：`python course/lessons/lesson1/solution/test_checks.py`
预期：`[FAIL] test_summarize_request_counts_steps ...`，`2/3 tests passed`。

- [ ] **步骤 3：实现 `summarize_request`**（替换桩体）

```python
def summarize_request(seq):
    """TODO(student): return (num_prompt_tokens, num_completion_tokens, total_steps).

    total_steps = number of engine steps this seq participated in (count _trace).
    It should equal num_completion_tokens — the prefill step itself emits the
    first token (scheduler.py:86-88), so every participating step appends one.
    """
    total_steps = sum(
        1 for r in _trace if any(b[0] == seq.seq_id for b in r["before"])
    )
    return (seq.num_prompt_tokens, seq.num_completion_tokens, total_steps)
```

- [ ] **步骤 4：运行测试，确认全过**

运行：`python course/lessons/lesson1/solution/test_checks.py`
预期：`3/3 tests passed`，退出码 0。

- [ ] **步骤 5：提交**

```bash
git add course/lessons/lesson1/solution/lab_solved.py course/lessons/lesson1/solution/test_checks.py
git commit -m "feat(course/lesson1): 实现 summarize_request + 合成场景 helper"
```

---

## Task 4：TDD `run_checks` + 接 `main`/`_print_trace`/`_check_env`

**文件：**
- 修改：`course/lessons/lesson1/solution/lab_solved.py`（替换 `run_checks` 和 `main` 桩，新增 `_print_trace`/`_check_env`）
- 测试：`course/lessons/lesson1/solution/test_checks.py`（追加两个测试）

**接口：**
- 产出：`run_checks(max_tokens) -> list[(name: str, ok: bool)]`；`main()` 装钩子、在两条 chat-template 过的 prompt 上跑 nano-vllm、打印 trace + 每请求摘要 + 检查、打印 `All checks passed ✓` 或退出 1。`main`/`_print_trace`/`_check_env` **不做单测**（需要真实环境）—— 由 Task 8 的端到端运行验证。

- [ ] **步骤 1：追加失败测试（在 `TESTS =` 行之前）**

```python
def test_run_checks_all_pass_on_valid_scenario():
    _build_valid_scenario()
    failed = [name for name, ok in lab.run_checks(max_tokens=5) if not ok]
    assert not failed, f"unexpected failures: {failed}"


def test_run_checks_catches_corrupt_prefill_nst():
    _build_valid_scenario()
    lab._trace[0]["before"][0] = (0, 999, "RUNNING")  # seq0 的 prefill nst 错了
    results = dict(lab.run_checks(max_tokens=5))
    assert results["Task2 prefill nst==num_prompt_tokens"] is False
```

- [ ] **步骤 2：运行测试，确认失败**

运行：`python course/lessons/lesson1/solution/test_checks.py`
预期：两个新测试都 FAIL（`NotImplementedError`），`3/5 tests passed`。

- [ ] **步骤 3：实现 `run_checks`**（替换桩）

```python
def run_checks(max_tokens):
    """Verify _trace against the request-lifecycle invariants."""
    results = []
    for seq_id, seq in _seqs.items():
        steps = [r for r in _trace if any(b[0] == seq_id for b in r["before"])]
        prefill_steps = [r for r in steps if r["is_prefill"]]
        finished = any(any(a[0] == seq_id and a[2] for a in r["after"]) for r in steps)
        # Task 1: state-machine trajectory
        results.append(("Task1 one prefill step", len(prefill_steps) == 1))
        results.append(("Task1 reached FINISHED", finished))
        results.append(("Task1 total_steps==num_completion_tokens",
                        len(steps) == seq.num_completion_tokens))
        # Task 2: num_scheduled_tokens
        prefill_nst = [b[1] for r in prefill_steps for b in r["before"] if b[0] == seq_id]
        results.append(("Task2 prefill nst==num_prompt_tokens",
                        prefill_nst == [seq.num_prompt_tokens]))
        decode_nst = [b[1] for r in steps if not r["is_prefill"]
                      for b in r["before"] if b[0] == seq_id]
        results.append(("Task2 decode nst all==1",
                        len(decode_nst) > 0 and all(n == 1 for n in decode_nst)))
        # Task 3: summarize_request
        _p, c, t = summarize_request(seq)
        results.append(("Task3 completion==len(completion_token_ids)",
                        c == len(seq.completion_token_ids)))
        results.append(("Task3 completion<=max_tokens", c <= max_tokens))
        results.append(("Task3 total_steps==num_completion_tokens",
                        t == seq.num_completion_tokens))
    return results
```

- [ ] **步骤 4：运行测试，确认全过**

运行：`python course/lessons/lesson1/solution/test_checks.py`
预期：`5/5 tests passed`，退出码 0。

- [ ] **步骤 5：实现 `main`、`_print_trace`、`_check_env`**（替换 `main` 桩，在 `if __name__` 上方加这两个 helper）

```python
def _check_env(model_path):
    missing = []
    try:
        import torch  # noqa: F401
    except Exception:
        missing.append("torch")
    try:
        import flash_attn  # noqa: F401
    except Exception:
        missing.append("flash-attn")
    if not os.path.isdir(model_path):
        missing.append(f"model at {model_path}")
    if missing:
        print("Missing prerequisites: " + ", ".join(missing))
        print("Run:\n  uv sync\n  huggingface-cli download Qwen/Qwen3-0.6B "
              "--local-dir " + model_path)
        raise SystemExit(1)


def _print_trace():
    print("\n--- trace (one row per engine step) ---")
    for i, r in enumerate(_trace):
        flag = "PREFILL" if r["is_prefill"] else "DECODE "
        nst = {b[0]: b[1] for b in r["before"]}
        print(f"step {i:2d} {flag} seqs={[b[0] for b in r['before']]} "
              f"nst={nst} new_tokens={r['token_ids']}")


def main():
    from nanovllm import LLM, SamplingParams
    from nanovllm.engine.scheduler import Scheduler
    from transformers import AutoTokenizer

    global _orig_postprocess, _orig_add
    model_path = os.path.expanduser("~/huggingface/Qwen3-0.6B/")
    max_tokens = 64

    _check_env(model_path)

    # 装钩子 —— 不改 nanovllm 源码
    _orig_postprocess = Scheduler.postprocess
    _orig_add = Scheduler.add
    Scheduler.postprocess = traced_postprocess
    Scheduler.add = traced_add

    tokenizer = AutoTokenizer.from_pretrained(model_path)
    llm = LLM(model_path, enforce_eager=True, tensor_parallel_size=1)
    sp = SamplingParams(temperature=0.6, max_tokens=max_tokens)
    raw = ["introduce yourself", "list all prime numbers within 100"]
    prompts = [tokenizer.apply_chat_template([{"role": "user", "content": p}],
                                             tokenize=False, add_generation_prompt=True)
               for p in raw]
    llm.generate(prompts, sp)

    _print_trace()
    print("\n--- per-request summary (Task 3) ---")
    for seq_id in sorted(_seqs):
        p, c, t = summarize_request(_seqs[seq_id])
        print(f"seq {seq_id}: prompt_tokens={p} completion_tokens={c} steps={t}")

    print("\n--- checks ---")
    results = run_checks(max_tokens)
    for name, ok in results:
        print(f"  [{'PASS' if ok else 'FAIL'}] {name}")
    if all(ok for _, ok in results):
        print("\nAll checks passed ✓")
    else:
        print("\nSome checks FAILED — review your TODO implementations.")
        raise SystemExit(1)
```

- [ ] **步骤 6：确认合成测试仍通过（import 无副作用）**

运行：`python course/lessons/lesson1/solution/test_checks.py`
预期：`5/5 tests passed`，退出码 0。（在没装 torch 的环境里能跑通，就证明 `import lab_solved` 没有触发 nanovllm import。）

- [ ] **步骤 7：提交**

```bash
git add course/lessons/lesson1/solution/lab_solved.py course/lessons/lesson1/solution/test_checks.py
git commit -m "feat(course/lesson1): 实现 run_checks + main 接线 + 环境守卫"
```

---

## Task 5：派生学生版 `lab.py`（挖空两个 TODO）

**文件：**
- 创建：`course/lessons/lesson1/lab.py`（拷贝 `solution/lab_solved.py`，替换两个 TODO 体）

**接口：**
- 产出：`lab.py` 与 `lab_solved.py` 完全一致，唯独 `traced_postprocess` 和 `summarize_request` 的函数体换成带引导注释的 `# TODO(student)` + `raise NotImplementedError(...)`。

- [ ] **步骤 1：创建 `lab.py`**

创建 `course/lessons/lesson1/lab.py`，**内容与 `solution/lab_solved.py` 完全相同**，但把 `traced_postprocess` 的体替换为：

```python
def traced_postprocess(self, seqs, token_ids, is_prefill):
    """TODO(student) — Task 1 (trace): record this step into _trace, then call
    the original via _orig_postprocess(self, seqs, token_ids, is_prefill).

    Capture each seq's num_scheduled_tokens BEFORE calling the original
    (postprocess resets it to 0 at scheduler.py:85). Suggested record:
        {"is_prefill": is_prefill,
         "before": [(s.seq_id, s.num_scheduled_tokens, s.status.name), ...],
         "token_ids": list(token_ids),
         "after":   [(s.seq_id, s.status.name, s.is_finished), ...]}
    """
    raise NotImplementedError("Task 1: fill in the trace recording")
```

……并把 `summarize_request` 的体替换为：

```python
def summarize_request(seq):
    """TODO(student) — Task 3 (small wrapper): return
    (num_prompt_tokens, num_completion_tokens, total_steps).

    total_steps = number of engine steps this seq participated in (count _trace).
    """
    raise NotImplementedError("Task 3: fill in summarize_request")
```

`traced_add`、`run_checks`、`main`、`_print_trace`、`_check_env` 和模块全局**保持与 `lab_solved.py` 一致，不动**。

- [ ] **步骤 2：确认 `lab.py` 能干净 import、且 TODO 在填之前不可达**

运行：`python -c "import sys; sys.path.insert(0,'course/lessons/lesson1'); import lab; print('import ok')"`
预期：`import ok`（证明没有顶层 nanovllm import）。

- [ ] **步骤 3：提交**

```bash
git add course/lessons/lesson1/lab.py
git commit -m "feat(course/lesson1): 加学生版 lab.py（两个引导式 TODO）"
```

---

## Task 6：写 `TUTORIAL.md`

**文件：**
- 创建：`course/lessons/lesson1/TUTORIAL.md`

**接口：**
- 无代码接口。内容由 Task 9 的审核角色检查，不做单测。

- [ ] **步骤 1：写 `TUTORIAL.md`**，包含以下章节（具体内容 + 已核验锚点；每节都要写成完整正文）：

  - **标题 + 一句话目标**：第 1 课——一个 request 从 `llm.generate` 到输出 token 的完整旅程。
  - **受众前提**：懂 LLM 推理原理、会读 Python（不必精通）、不熟 vLLM/nano-vllm。
  - **§0 环境初始化**：`uv sync`（根 `pyproject.toml` 已声明 torch≥2.4/triton≥3.0/transformers≥4.51/flash-attn/xxhash）→ `huggingface-cli download Qwen/Qwen3-0.6B --local-dir ~/huggingface/Qwen3-0.6B/` → `uv run python example.py` 跑通当 smoke。flash-attn 装不上时：给 `pip install flash-attn --no-build-isolation` 备选，并提示需匹配 CUDA/torch 版本。
  - **§1 学完你能**：(a) 说清 request 全旅程；(b) 看懂 `step()` 三段式；(c) 解释 `Sequence` 作主进程↔GPU worker 唯一 IPC 载体；(d) 解释 `num_tokens` 正负号双语义。
  - **§2 全景图**：用文字 + ASCII 画 `generate → add_request → [while not is_finished(): step()] → schedule → run → postprocess → decode → output`，并列出 Sequence 状态机 `WAITING → RUNNING → FINISHED`。
  - **§3 逐段讲（锚 file:line）**：逐条讲解并贴关键代码——`generate` `llm_engine.py:60-90`；`add_request→Sequence` `llm_engine.py:43-48` + `sequence.py:18-31`；`step()` 三段式 `llm_engine.py:49-55`；IPC `__getstate__/__setstate__` `sequence.py:72-83`（decode 传单个 `last_token`、prefill 传完整 `token_ids`）；`postprocess` `scheduler.py:81-92`；**关键非显然点**：prefill 一次算完时 `continue` 不触发、`append_token` 照跑——prefill 步也产出第 1 个 token（`scheduler.py:86-88`），故每参与一步 append 一个 token；`num_tokens` 正负号 `llm_engine.py:51` 驱动两条吞吐曲线 `llm_engine.py:76-79`；三态机 `sequence.py:8-11`。
  - **§4 进 lab**：指向 `lab.py`，说明两个 TODO（Task 1 填 `traced_postprocess`、Task 3 填 `summarize_request`），Task 2 是读 trace 写解释；运行 `uv run python lab.py`，全过打印 `All checks passed ✓`。
  - **§5 图示补充**：ASCII 状态机图、step 时序图、prefill vs decode 的 `num_scheduled_tokens` 对比（prefill=整条 prompt、decode=1）。

- [ ] **步骤 2：提交**

```bash
git add course/lessons/lesson1/TUTORIAL.md
git commit -m "docs(course/lesson1): 写 TUTORIAL.md（环境初始化 + request 生命周期）"
```

---

## Task 7：写 `solution/ANSWERS.md`

**文件：**
- 创建：`course/lessons/lesson1/solution/ANSWERS.md`

- [ ] **步骤 1：写 `ANSWERS.md`**，分三部分：

  - **Task 1 参考实现**：贴 `solution/lab_solved.py` 里 `traced_postprocess` 的完整代码；给出 example.py 两条 prompt 的**期望 trace 形态**：step 0 = `PREFILL seqs=[0,1] nst={0:<prompt0>,1:<prompt1>}`（两条同一步 prefill），其后若干 `DECODE seqs=[0,1] nst={0:1,1:1}`，某步起 seq0 命中 eos 被 `running.remove`、只剩 seq1 继续 decode 直到 max_tokens。
  - **Task 2 explain 参考答案**：解释 `num_tokens = sum(seq.num_scheduled_tokens ...) if is_prefill else -len(seqs)`（`llm_engine.py:51`）——正数=本步 prefill 的 token 总数、负数编码"这是 decode 且本步每 seq 1 个"；正负号驱动 `generate` 里 `prefill_throughput`/`decode_throughput` 两条曲线（`llm_engine.py:76-79`）。判分要点：必须同时点出"正负号区分 prefill/decode"与"绝对值=本步 token 计数"两点。
  - **Task 3 参考实现**：贴 `summarize_request` 代码；说明为何 `total_steps == num_completion_tokens`（prefill 步也产 1 个 token，见 §3）。TODO→答案映射：`lab.py` 的两个 TODO 直接对应 `lab_solved.py` 的两个函数体。

- [ ] **步骤 2：提交**

```bash
git add course/lessons/lesson1/solution/ANSWERS.md
git commit -m "docs(course/lesson1): 写 solution/ANSWERS.md（参考答案 + 判分要点）"
```

---

## Task 8：参考答案的端到端运行（依赖环境）

**文件：**
- 不改任何文件。在真实 GPU 上验证 `solution/lab_solved.py`。

**接口：**
- 消费：一个可用的 nano-vllm 环境（torch/triton/flash-attn + Qwen3-0.6B + 一张 CUDA 卡）。

> **此任务依赖环境。** 本服务器 base 环境缺 torch/triton/flash-attn 与模型（见 `course/README.md` 前置依赖）。若 `uv sync` 或模型下载失败（通常是 flash-attn），**停下并把阻塞反馈给用户**——不要标记本任务完成，也不要伪造输出。

- [ ] **步骤 1：装环境**

运行：`uv sync`
预期：创建 `.venv`，安装 torch/triton/transformers/flash-attn/xxhash，退出码 0。flash-attn 失败则试 `uv pip install flash-attn --no-build-isolation`，仍失败则反馈用户。

- [ ] **步骤 2：下载模型**

运行：`huggingface-cli download Qwen/Qwen3-0.6B --local-dir ~/huggingface/Qwen3-0.6B/`
预期：模型文件出现在 `~/huggingface/Qwen3-0.6B/` 下。

- [ ] **步骤 3：端到端运行参考答案**

运行：`uv run python course/lessons/lesson1/solution/lab_solved.py`
预期：打印 `--- trace ---` 表、`--- per-request summary ---`、`--- checks ---`（全 `PASS`），最后 `All checks passed ✓`，退出码 0。

- [ ] **步骤 4：跑学生版 `lab.py`，确认 TODO 未填时 fail-closed**

运行：`uv run python course/lessons/lesson1/lab.py`
预期：在第一个 TODO 处抛 `NotImplementedError` 非零退出（证明学生必须先填）。步骤 3 已证明填好后能过，无需再临时拷贝验证。

- [ ] **步骤 5：提交**（仅当需要改 tracked 文件做环境修复时；否则不提交）

```bash
# 仅当环境工作改动了 tracked 文件：
git add -A && git commit -m "chore(course/lesson1): 端到端运行所需的环境修复"
```

---

## Task 9：审核 + 学生试课验收（三角色闸门）

**文件：**
- 除非审核/试课暴露需要改的地方，否则不改文件。

**接口：**
- 消费：Task 1–8 的全部产物。

这是 spec §9 的三角色闸门。是否真用 subagent 跑循环 vs 内联分饰，在执行时定。

- [ ] **步骤 1：审核（课程架构审核）** —— 逐条对照源码核：
  - 目标清晰、知识递进合理（§1→§2→§3→lab 顺序）、tut:lab 比例合适；
  - 每个 lab 任务有明确输入/输出/验证（Task1：`_trace` 记录；Task2：trace 标注+解释；Task3：`(prompt,completion,steps)`）；
  - TUTORIAL 每个 `file:line` 锚点准确（通读对应源码段核对）；
  - lab 是否真帮理解 request 全流程，而非为留空而留空。
  记录任何 blocking 问题。

- [ ] **步骤 2：学生试课** —— 在 Task 8 的环境上，以学习者身份真做一遍 lab：
  - 跟 §0 装环境、§1–§3 读讲义、§4 做 lab（填两个 TODO、跑 `uv run python lab.py`）；
  - 记录卡点：概念跳步 / 接口不清 / 验证不足 / 环境配置 / 错误提示不友好；
  - 必须亲手跑出 `All checks passed ✓`。

- [ ] **步骤 3：迭代修改** —— 把审核 + 学生问题反馈给工程师；改；再审/再试。重复到审核通过**且**学生试课无 blocking 卡点。

- [ ] **步骤 4：提交修改并标记完成**

```bash
git add -A && git commit -m "docs(course/lesson1): 应用审核 + 学生试课反馈"
```

**整份计划验收**：Task 1–7 完成；Task 8 在真实环境打印 `All checks passed ✓`；Task 9 审核通过 + 学生试课无阻塞。
