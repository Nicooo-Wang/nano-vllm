# 第 2 课（nano-vllm 课程）实现计划

> **给 agentic worker：** 必需子技能：用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 按任务逐个实现本计划。步骤用复选框（`- [ ]`）跟踪。

**目标：** 构建第 2 课的可运行内容（`TUTORIAL.md` + 学生版 `lab.py` + `solution/`），用 trace/observe/explain + 两个 construct 任务（`prefill_slot_mapping` 几何 + `simulate_fa_calls` 手动调 FA）讲清 KV 缓存的接口与 paged 物理形态，并内置可运行的验证。

**架构：** `lab.py` 用 monkeypatch 替换 `Scheduler.postprocess`（快照 `block_table`）与 `Attention.forward`（layer-0 抓 FA 真实调用，**不动源码**），在两条 prompt 上跑 nano-vllm（`enforce_eager=True`），用 `run_checks()` 断言 trace / slot 几何 / FA 调用不变式，打印 `All checks passed ✓`。学生填三个 TODO（`traced_attention_forward` hook + `prefill_slot_mapping` + `simulate_fa_calls`）。Task 3（手动调 FA）走注入 callable 设计：`simulate_fa_calls(fa_varlen, fa_kvcache, device, dtype)`——main 传真实 flash_attn（端到端验 shape + 值交叉验证），合成单测验 mock 验调用契约 + 失败诊断。Q3b（cudagraph `-1` 哨兵）用**另一次进程调用** `lab_solved.py --cudagraph`（同进程第二个 `LLM()` 会撞 NCCL init，见 `llm_engine.py:31` + `model_runner.py:26`）。验证逻辑（`run_checks`/`_verify_fa_calls`/三个 TODO）的 import 不依赖 nanovllm/flash_attn（torch 仅在函数内 / mock 路径），可用合成数据做 **GPU-free 单测**；真实端到端（H200 上跑出 `All checks passed ✓` + 哨兵证据）是最后依赖环境的任务。

**技术栈：** Python 3.10+、nano-vllm（torch/triton/flash-attn/transformers）、uv、仅标准库的单测（不引入 pytest 依赖）。

## 关于 GPU / H200 的说明（重要，先读）

- **第 2 课的 lab 主体必须在 H200（或任意 CUDA 卡）上跑。** 学生执行 `uv run python lab.py` 时，nano-vllm 加载 Qwen3-0.6B、在 GPU 上 prefill/decode，trace 才会被捕获；`simulate_fa_calls` 的真实 flash_attn 调用也只在 GPU 上跑（需 fp16/bf16 + CUDA）。`All checks passed ✓` 由 Task 9 在 H200 上兑现，**不可省略、不能用合成数据替代**。
- 本计划里凡是"GPU-free"，**仅指 Task 1–5 的合成单测**：用假 `_trace`/`_seq_blocks`/mock FA 测"检查代码与 TODO 函数本身"是否正确，**不跑 nano-vllm、不要 GPU**。
- **两层都不可少**：合成单测证明**检查代码 + 调用契约逻辑**对；H200 运行证明**真实 trace + 真实 FA 语义**与假设一致。
- **Q3b 的 cudagraph 观察**是**第二次进程调用**（`--cudagraph`）：同进程构造第二个 `LLM()` 会重复 `dist.init_process_group`（`model_runner.py:26`）报错，故必须新开进程。`capture_cudagraph` 在本卡（H200）若失败，`main_cudagraph` 走 explain-from-code fallback（spec D1），不视为阻塞。

## 全局约束

- **不要修改任何 `nanovllm/` 源码** —— 只在 `lab.py`/`lab_solved.py` 里用 monkeypatch 观察。
- **`lab.py` 和 `solution/lab_solved.py` 的顶层 import 不能依赖 nanovllm/torch/flash_attn** —— nanovllm/transformers/flash_attn 的 import 都放在 `main()`/`main_cudagraph()` 内部；`simulate_fa_calls` 用**函数内** `import torch`。这是合成单测（含 mock FA）能 GPU-free 的前提。
- **学生可见目录只放 `TUTORIAL.md` + `lab.py`**；其余（参考答案/合成单测/答案）都在 `solution/` 下。
- 运行配置：`tensor_parallel_size=1`；Run 1 `enforce_eager=True`（抓 prefill+decode FA 调用），Run 2 `--cudagraph` 即 `enforce_eager=False`（观察哨兵）；模型在 `~/huggingface/Qwen3-0.6B/`；`max_tokens=4`（缩短 lab）。
- 合成单测用 **`python`（不是 `uv run`）** 跑；端到端运行才用 `uv run`/`.venv/bin/python`。
- 每个 task 一个 `git commit`。分支：`course/lesson2`（已建）。
- 运行时打印保持**英文**（同 L01）；注释/docstring/讲义正文用中文。
- block_size 固定 256（`config.py:17` 默认，`assert %256==0`）。

## 文件结构

- `course/lessons/lesson2/solution/lab_solved.py` — 参考答案：模块全局（`_trace`/`_seq_blocks`/`_orig_*`/`_get_context`/`_layer_order`/TOY 常量）、`FAContractError`、`traced_postprocess`（提供，快照 block_table）、`traced_attention_forward`（Task1 TODO）、`prefill_slot_mapping`（Task2 TODO）、`simulate_fa_calls`（Task3 TODO）、`_verify_fa_calls`（提供）、`run_checks`（提供）、`main`/`main_cudagraph`/`_check_env`/`_print_trace`（提供）。
- `course/lessons/lesson2/solution/test_checks.py` — 合成单测（标准库 + 函数内 torch；mock FA；无 GPU）。
- `course/lessons/lesson2/lab.py` — 学生版：= lab_solved，但三个 TODO 体换成引导式 `# TODO(student)` + `raise NotImplementedError`。
- `course/lessons/lesson2/TUTORIAL.md` — 讲义 + §0 环境（指向 L01 §0）+ §3 含两个 FA 签名的 worked example。
- `course/lessons/lesson2/solution/ANSWERS.md` — Task4 explain 答案 + 判分要点 + 期望 trace + TODO→答案映射。

---

## Task 1：搭 `lab_solved.py` 骨架 + 测试 harness + 提供版 `traced_postprocess`

**文件：**
- 创建：`course/lessons/lesson2/solution/lab_solved.py`
- 创建：`course/lessons/lesson2/solution/test_checks.py`
- 测试：`course/lessons/lesson2/solution/test_checks.py`

**接口：**
- 产出：模块全局 `_trace: list`、`_seq_blocks: dict`、`_orig_attention_forward`、`_orig_postprocess`、`_get_context`、`_layer_order: dict`；常量 `TOY_NUM_HEADS=4`/`TOY_NUM_KV_HEADS=1`/`TOY_HEAD_DIM=64`/`BLOCK_SIZE=256`；异常 `FAContractError`；提供版 `traced_postprocess(self, seqs, token_ids, is_prefill)`（**入口**快照每 seq 的 `block_table` 进 `_seq_blocks`，再调 `_orig_postprocess` 透传——必须在调原始前抓，因为 postprocess 内部对 FINISHED seq 调 `deallocate` 会 `block_table.clear()`，`block_manager.py:101`）。其余函数（`traced_attention_forward`/`prefill_slot_mapping`/`simulate_fa_calls`/`_verify_fa_calls`/`run_checks`/`main`/`main_cudagraph`）为 `raise NotImplementedError` 桩。

- [ ] **步骤 1：写 `lab_solved.py` 骨架**

```python
"""Lesson 2 lab (reference solution) — flash-attention calling contracts + paged form.

The student file `../lab.py` is this file with the three TODOs blanked.
Importing this module does NOT import nanovllm/torch/flash_attn (those live in
main()/main_cudagraph(); simulate_fa_calls does a function-local `import torch`),
so the synthetic tests in test_checks.py run without a GPU.
"""
import sys

# ---- toy constants for Task 3 (simulate_fa_calls) ----
TOY_NUM_HEADS = 4
TOY_NUM_KV_HEADS = 1
TOY_HEAD_DIM = 64
BLOCK_SIZE = 256   # config.kvcache_block_size default (config.py:17)

_trace = []                 # one record per engine step (layer 0's Attention.forward call)
_seq_blocks = {}            # seq_id -> list[block_id]  (snapshot of block_table per step)
_orig_attention_forward = None
_orig_postprocess = None
_get_context = None         # set in main() to nanovllm.utils.context.get_context
_layer_order = {}           # id(Attention module) -> layer index (only idx 0 records)


class FAContractError(Exception):
    """Raised when simulate_fa_calls mis-calls flash-attention (wrong args / wrong semantics).
    Carries a diagnostic hint pointing at the likely fix + the attention.py line to mirror."""


def traced_postprocess(self, seqs, token_ids, is_prefill):
    """Hook for Scheduler.postprocess (PROVIDED, not a student TODO).

    Snapshot each seq's block_table at ENTRY, before the original postprocess runs:
    finished seqs get deallocate()'d inside postprocess (scheduler.py:91) which calls
    block_table.clear() (block_manager.py:101), so reading it post-run would be empty.
    """
    for s in seqs:
        _seq_blocks[s.seq_id] = list(s.block_table)
    return _orig_postprocess(self, seqs, token_ids, is_prefill)


def traced_attention_forward(self, q, k, v):
    raise NotImplementedError("Task 1: fill in the FA-call trace recording")


def prefill_slot_mapping(block_table, block_size, start, num_tokens):
    raise NotImplementedError("Task 2: fill in prefill_slot_mapping")


def simulate_fa_calls(fa_varlen, fa_kvcache, device, dtype):
    raise NotImplementedError("Task 3: fill in the two flash-attention calls")


def _verify_fa_calls(prefill_out, decode_out):
    raise NotImplementedError("Task 4b: fill in _verify_fa_calls")


def run_checks(max_tokens, fa_results=None, block_size=BLOCK_SIZE):
    raise NotImplementedError("Task 5: fill in run_checks")


def _check_env(model_path):
    raise NotImplementedError("Task 5")


def _print_trace():
    raise NotImplementedError("Task 5")


def main():
    raise NotImplementedError("Task 5")


def main_cudagraph():
    raise NotImplementedError("Task 5")


if __name__ == "__main__":
    if "--cudagraph" in sys.argv:
        main_cudagraph()
    else:
        main()
```

- [ ] **步骤 2：写 `test_checks.py` harness + `traced_postprocess` 测试（应通过）**

```python
"""Synthetic unit tests for lab_solved (no GPU, no nano-vllm, no flash-attn).
Run: python solution/test_checks.py
"""
import sys, os, types
sys.path.insert(0, os.path.dirname(__file__))
import lab_solved as lab


def test_traced_postprocess_snapshots_block_table_before_original():
    lab._seq_blocks = {}
    seq = types.SimpleNamespace(seq_id=7, block_table=[3, 8])
    cleared = []  # original postprocess will clear block_table (block_manager.py:101)

    def fake_orig(self, seqs, token_ids, is_prefill):
        for s in seqs:
            s.block_table.clear()   # mimic deallocate clearing it
            cleared.append(s.seq_id)
        return "done"

    lab._orig_postprocess = fake_orig
    out = lab.traced_postprocess("SELF", [seq], [42], True)

    assert out == "done", "must call original and pass through its return value"
    assert cleared == [7], "original must run exactly once"
    assert lab._seq_blocks[7] == [3, 8], "must snapshot block_table BEFORE original clears it"


TESTS = [v for k, v in sorted(globals().items()) if k.startswith("test_")]

if __name__ == "__main__":
    failures = 0
    for t in TESTS:
        try:
            t(); print(f"[PASS] {t.__name__}")
        except (AssertionError, NotImplementedError) as e:  # NotImplementedError → FAIL, not crash
            failures += 1; print(f"[FAIL] {t.__name__}: {e}")
    print(f"\n{len(TESTS)-failures}/{len(TESTS)} tests passed")
    sys.exit(1 if failures else 0)
```

- [ ] **步骤 3：运行测试，确认通过**（骨架已实现 `traced_postprocess`）

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：先 `[PASS] test_traced_postprocess_snapshots_block_table_before_original`，再 `1/1 tests passed`，退出码 0。

- [ ] **步骤 4：提交**

```bash
git add course/lessons/lesson2/solution/lab_solved.py course/lessons/lesson2/solution/test_checks.py
git commit -m "feat(course/lesson2): 搭 lab_solved 骨架 + FAContractError + traced_postprocess + 测试 harness"
```

---

## Task 2：TDD `traced_attention_forward`（lab Task 1 —— FA 调用 trace）

**文件：**
- 修改：`course/lessons/lesson2/solution/lab_solved.py`（替换 `traced_attention_forward` 桩）
- 测试：`course/lessons/lesson2/solution/test_checks.py`（追加测试）

**接口：**
- 消费：`_get_context()`（main 里设成 `nanovllm.utils.context.get_context`；测试里设成 fake），`_orig_attention_forward`（测试里设成 fake），`_layer_order`。context 对象需暴露 `.is_prefill`/`.cu_seqlens_q`/`.cu_seqlens_k`/`.max_seqlen_q`/`.max_seqlen_k`/`.context_lens`/`.block_tables`/`.slot_mapping`（后几个带 `.tolist()` 或为 None）。q/k/v 需有 `.shape`。
- 产出：往 `_trace` 追加一条 dict（**仅 layer 0**，用 `_layer_order.setdefault(id(self), len(_layer_order))==0` 判定），形如 `{"is_prefill": bool, "q_shape": tuple, "k_shape": tuple, "v_shape": tuple, "cu_seqlens_q": list|None, "cu_seqlens_k": list|None, "max_seqlen_q": int, "max_seqlen_k": int, "context_lens": list|None, "block_tables": tuple|None, "slot_mapping": list}`，然后调 `_orig_attention_forward(self, q, k, v)` 透传返回。

- [ ] **步骤 1：写失败测试（追加到 `test_checks.py` 的 `TESTS =` 行之前）**

```python
def _fake_context(is_prefill):
    return types.SimpleNamespace(
        is_prefill=is_prefill,
        cu_seqlens_q=types.SimpleNamespace(tolist=lambda: [0, 3, 5]),
        cu_seqlens_k=types.SimpleNamespace(tolist=lambda: [0, 3, 5]),
        max_seqlen_q=3, max_seqlen_k=3,
        context_lens=types.SimpleNamespace(tolist=lambda: [3, 2]),
        block_tables=types.SimpleNamespace(shape=(2, 1)),
        slot_mapping=types.SimpleNamespace(tolist=lambda: [10, 11, 12, 13, 14]),
    )


def _fake_qkv(shape):
    return types.SimpleNamespace(shape=shape)


def test_traced_attention_forward_records_only_layer0_and_calls_original():
    lab._trace = []
    lab._layer_order = {}
    lab._get_context = lambda: _fake_context(is_prefill=True)
    returned = []
    lab._orig_attention_forward = lambda self, q, k, v: returned.append("orig") or "OUT"

    layer0 = object()                       # first Attention module
    layer1 = object()                       # second Attention module
    out = lab.traced_attention_forward(layer0, _fake_qkv((5, 4, 64)), _fake_qkv((5, 1, 64)), _fake_qkv((5, 1, 64)))
    lab.traced_attention_forward(layer1, _fake_qkv((5, 4, 64)), _fake_qkv((5, 1, 64)), _fake_qkv((5, 1, 64)))

    assert out == "OUT" and returned == ["orig", "orig"], "original runs once per layer (only recording is layer-0-gated) and passes through"
    assert len(lab._trace) == 1, "only layer 0 records; layer 1 must be skipped"
    rec = lab._trace[0]
    assert rec["is_prefill"] is True
    assert rec["q_shape"] == (5, 4, 64) and rec["k_shape"] == (5, 1, 64)
    assert rec["cu_seqlens_q"] == [0, 3, 5]
    assert rec["context_lens"] is None      # prefill record does not store context_lens
    assert rec["block_tables"] == (2, 1)
    assert rec["slot_mapping"] == [10, 11, 12, 13, 14]
    # second call from the SAME layer0 must also record (one trace row per step)
    lab.traced_attention_forward(layer0, _fake_qkv((5, 4, 64)), _fake_qkv((5, 1, 64)), _fake_qkv((5, 1, 64)))
    assert len(lab._trace) == 2
```

- [ ] **步骤 2：运行测试，确认失败**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：`[FAIL] test_traced_attention_forward_records_only_layer0_and_calls_original ...`（NotImplementedError），`1/2 tests passed`。

- [ ] **步骤 3：实现 `traced_attention_forward`**（替换桩体）

```python
def traced_attention_forward(self, q, k, v):
    """TODO(student) — Task 1: record this step's flash-attention call into _trace,
    then call _orig_attention_forward(self, q, k, v).

    Attention.forward fires once per layer per step (num_hidden_layers times); every
    layer in a step shares the same context & shapes, so only LAYER 0 records.
    """
    if _layer_order.setdefault(id(self), len(_layer_order)) == 0:
        context = _get_context()
        is_prefill = context.is_prefill
        _trace.append({
            "is_prefill": is_prefill,
            "q_shape": tuple(q.shape), "k_shape": tuple(k.shape), "v_shape": tuple(v.shape),
            "cu_seqlens_q": context.cu_seqlens_q.tolist() if is_prefill else None,
            "cu_seqlens_k": context.cu_seqlens_k.tolist() if is_prefill else None,
            "max_seqlen_q": context.max_seqlen_q, "max_seqlen_k": context.max_seqlen_k,
            "context_lens": context.context_lens.tolist() if not is_prefill else None,
            "block_tables": (tuple(context.block_tables.shape)
                             if context.block_tables is not None else None),
            "slot_mapping": context.slot_mapping.tolist(),
        })
    return _orig_attention_forward(self, q, k, v)
```

- [ ] **步骤 4：运行测试，确认两个都过**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：`2/2 tests passed`，退出码 0。

- [ ] **步骤 5：提交**

```bash
git add course/lessons/lesson2/solution/lab_solved.py course/lessons/lesson2/solution/test_checks.py
git commit -m "feat(course/lesson2): 实现 traced_attention_forward（layer-0 FA 调用 trace）"
```

---

## Task 3：TDD `prefill_slot_mapping`（lab Task 2 —— paged slot 几何）

**文件：**
- 修改：`course/lessons/lesson2/solution/lab_solved.py`（替换 `prefill_slot_mapping` 桩）
- 测试：`course/lessons/lesson2/solution/test_checks.py`（追加测试）

**接口：**
- 产出：`prefill_slot_mapping(block_table: list[int], block_size: int, start: int, num_tokens: int) -> list[int]`，忠实复现 `model_runner.py:151-161`：`start_block=start//block_size`、`end_block=ceil((start+num_tokens)/block_size)`；逐块 `slot_start=block_table[i]*block_size`，**首块** `+= start%block_size`，**非末块** `slot_end=block_table[i]*block_size+block_size`，**末块** `slot_end=block_table[i]*block_size + (start+num_tokens) - i*block_size`；`slots.extend(range(slot_start, slot_end))`。返回长度 == `num_tokens`。

- [ ] **步骤 1：写失败测试（在 `TESTS =` 行之前）**

```python
def test_prefill_slot_mapping_fresh_noncontiguous():
    # 3 blocks of 256, non-contiguous block ids so scatter is visible; start=0
    got = lab.prefill_slot_mapping([7, 3, 9], 256, start=0, num_tokens=600)
    expected = (list(range(7 * 256, 7 * 256 + 256))      # block 7, full (256)
                + list(range(3 * 256, 3 * 256 + 256))    # block 3, full (256)
                + list(range(9 * 256, 9 * 256 + 88)))    # block 9, last truncated (600-512=88)
    assert got == expected
    assert len(got) == 600


def test_prefill_slot_mapping_chunked_first_block_offset():
    # start=300 → first block offset += 300%256=44; last block truncated to end - i*block_size
    got = lab.prefill_slot_mapping([7, 3, 9], 256, start=300, num_tokens=256)   # end=556
    expected = (list(range(3 * 256 + 44, 3 * 256 + 256))    # block 3 (start_block=1), 212 slots
                + list(range(9 * 256, 9 * 256 + 44)))       # block 9 (last), 556-512=44 slots
    assert got == expected
    assert len(got) == 256
```

- [ ] **步骤 2：运行测试，确认失败**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：两个新测试都 FAIL（NotImplementedError），`2/4 tests passed`。

- [ ] **步骤 3：实现 `prefill_slot_mapping`**（替换桩体）

```python
def prefill_slot_mapping(block_table, block_size, start, num_tokens):
    """TODO(student) — Task 2: reproduce model_runner.py:151-161 prefill slot scatter.

    Each token maps to physical slot block_id*block_size + in-block offset. The first
    block's start is further offset by start%block_size; the last block is truncated to
    end - i*block_size.
    """
    end = start + num_tokens
    start_block = start // block_size
    end_block = (end + block_size - 1) // block_size
    slots = []
    for i in range(start_block, end_block):
        slot_start = block_table[i] * block_size
        if i == start_block:
            slot_start += start % block_size
        if i != end_block - 1:
            slot_end = block_table[i] * block_size + block_size
        else:
            slot_end = block_table[i] * block_size + end - i * block_size
        slots.extend(range(slot_start, slot_end))
    return slots
```

- [ ] **步骤 4：运行测试，确认全过**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：`4/4 tests passed`，退出码 0。

- [ ] **步骤 5：提交**

```bash
git add course/lessons/lesson2/solution/lab_solved.py course/lessons/lesson2/solution/test_checks.py
git commit -m "feat(course/lesson2): 实现 prefill_slot_mapping（paged slot 三段偏移几何）"
```

---

## Task 4：TDD `_verify_fa_calls` + `simulate_fa_calls`（lab Task 3 —— 手动调 FA + UT + 诊断）

**文件：**
- 修改：`course/lessons/lesson2/solution/lab_solved.py`（替换 `_verify_fa_calls` 和 `simulate_fa_calls` 桩）
- 测试：`course/lessons/lesson2/solution/test_checks.py`（追加 mock harness + 4 个测试）

**接口：**
- 消费：`fa_varlen`/`fa_kvcache`（main 传真实 `flash_attn_varlen_func`/`flash_attn_with_kvcache`；测试传 mock）；`device`/`dtype`。
- 产出：`simulate_fa_calls(fa_varlen, fa_kvcache, device, dtype) -> (prefill_out, decode_out)`，toy 设置 2 seqs 长 `[3,2]`、`num_heads=4`/`num_kv_heads=1`/`head_dim=64`；prefill 调 `fa_varlen(q,k,v, cu_seqlens_q=, cu_seqlens_k=, max_seqlen_q=3, max_seqlen_k=3, softmax_scale=head_dim**-0.5, causal=True)`；decode 令 `q_dec=stack([q[2],q[4]])`、cache `(2,4,1,64)` 切片预填、`block_table=[[0],[1]]`、`cache_seqlens=[3,2]`、调 `fa_kvcache(q_dec.unsqueeze(1), k_cache, v_cache, cache_seqlens=, block_table=, softmax_scale=, causal=True).squeeze(1)`。调用包 try/except：`FAContractError` 直抛、其它 `Exception` 重抛 `FAContractError("...mirror attention.py:67-70/72-74...")`。
- 产出：`_verify_fa_calls(prefill_out, decode_out) -> list[(name:str, ok:bool)]`，三条：prefill shape `(5,4,64)`、decode shape `(2,4,64)`、`allclose(decode_out, stack([prefill_out[2],prefill_out[4]]), atol=1e-2)`（值交叉验证）。

- [ ] **步骤 1：写 mock harness + 4 个失败测试（在 `TESTS =` 行之前）**

```python
def _make_mock_fa(device="cpu", dtype=None):
    """Mock fa_varlen/fa_kvcache that (a) validate the calling contract at call time and
    (b) return cross-check-consistent stubs. Uses function-local torch (no GPU needed)."""
    import torch
    if dtype is None:
        dtype = torch.float32
    state = {}
    total, num_heads, head_dim = 5, lab.TOY_NUM_HEADS, lab.TOY_HEAD_DIM

    def mock_varlen(q, k, v, *, cu_seqlens_q, cu_seqlens_k, max_seqlen_q, max_seqlen_k,
                    softmax_scale, causal):
        assert q.ndim == 3, "varlen q must be packed (total, nheads, head_dim), not per-seq padded"
        assert tuple(q.shape)[0] == cu_seqlens_q[-1], "cu_seqlens_q[-1] must equal total tokens"
        assert causal is True
        state["prefill"] = torch.randn(total, num_heads, head_dim, device=device, dtype=dtype)
        return state["prefill"]

    def mock_kvcache(q, k_cache, v_cache, *, cache_seqlens, block_table,
                     softmax_scale, causal):
        assert q.ndim == 4 and tuple(q.shape)[1] == 1, \
            "kvcache q must be (batch, seqlen=1, nheads, head_dim) — did you q_dec.unsqueeze(1)?"
        assert block_table.ndim == 2, "block_table must be 2-D (seq -> physical blocks)"
        assert cache_seqlens.ndim == 1, "cache_seqlens must be 1-D (valid KV count per seq)"
        pf = state["prefill"]
        # cross-check-consistent stub: decode reads the same KV as prefill's last token
        return torch.stack([pf[2], pf[4]]).unsqueeze(1)   # (2,1,num_heads,head_dim)

    return mock_varlen, mock_kvcache


def test_simulate_fa_calls_correct_contract():
    import torch
    mv, mk = _make_mock_fa()
    prefill_out, decode_out = lab.simulate_fa_calls(mv, mk, "cpu", torch.float32)
    # _verify_fa_calls should pass on a correct call
    failed = [n for n, ok in lab._verify_fa_calls(prefill_out, decode_out) if not ok]
    assert not failed, f"correct call should verify clean, failed: {failed}"


def test_simulate_fa_calls_wraps_fa_errors():
    import torch

    def raising(*a, **k):
        raise RuntimeError("fake flash_attn shape error")

    try:
        lab.simulate_fa_calls(raising, raising, "cpu", torch.float32)
        assert False, "should have raised FAContractError"
    except lab.FAContractError as e:
        assert "attention.py" in str(e), "diagnostic must point at attention.py to mirror"


def test_verify_fa_calls_rejects_value_mismatch():
    import torch
    prefill_out = torch.randn(5, 4, 64)
    decode_out = torch.randn(2, 4, 64)   # NOT equal to stack([prefill_out[2], prefill_out[4]])
    failed = [n for n, ok in lab._verify_fa_calls(prefill_out, decode_out) if not ok]
    assert any("cache_seqlens" in n or "allclose" in n for n in failed), \
        "value mismatch must flag the cross-check naming cache_seqlens/block_table"


def test_verify_fa_calls_rejects_bad_prefill_shape():
    import torch
    bad_prefill = torch.randn(7, 4, 64)   # wrong total dim
    decode_out = torch.randn(2, 4, 64)
    failed = [n for n, ok in lab._verify_fa_calls(bad_prefill, decode_out) if not ok]
    assert any("prefill out shape" in n for n in failed)
```

- [ ] **步骤 2：运行测试，确认失败**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：4 个新测试都 FAIL（NotImplementedError），`4/8 tests passed`。

- [ ] **步骤 3：实现 `_verify_fa_calls` + `simulate_fa_calls`**（替换两个桩）

```python
def _verify_fa_calls(prefill_out, decode_out):
    """Check simulate_fa_calls outputs (PROVIDED). Returns list[(name, ok)]."""
    import torch
    total, num_seqs, num_heads, head_dim = 5, 2, TOY_NUM_HEADS, TOY_HEAD_DIM
    results = []
    results.append(("Task3 simulate prefill out shape (total,H,D)",
                    tuple(prefill_out.shape) == (total, num_heads, head_dim)))
    results.append(("Task3 simulate decode out shape (num_seqs,H,D)",
                    tuple(decode_out.shape) == (num_seqs, num_heads, head_dim)))
    expected = torch.stack([prefill_out[2], prefill_out[4]])
    ok = (decode_out.shape == expected.shape
          and torch.allclose(decode_out, expected, atol=1e-2))
    results.append(("Task3 decode out == prefill last-token (allclose) "
                    "— check cache_seqlens/block_table vs cache prefill", ok))
    return results


def simulate_fa_calls(fa_varlen, fa_kvcache, device, dtype):
    """TODO(student) — Task 3: call the two flash-attention interfaces on toy inputs.
    fa_varlen/fa_kvcache are passed in (real flash_attn from main(); mocks in tests) —
    you only need to CALL them correctly. Focus: what args differ between prefill & decode.
    Returns (prefill_out, decode_out). Mirror attention.py:67-70 (prefill) / 72-74 (decode).
    """
    import torch
    num_heads, num_kv_heads, head_dim = TOY_NUM_HEADS, TOY_NUM_KV_HEADS, TOY_HEAD_DIM
    scale = head_dim ** -0.5
    # toy inputs (provided): 2 seqs of lengths [3, 2]
    q = torch.randn(5, num_heads, head_dim, device=device, dtype=dtype)
    k = torch.randn(5, num_kv_heads, head_dim, device=device, dtype=dtype)
    v = torch.randn(5, num_kv_heads, head_dim, device=device, dtype=dtype)
    cu_seqlens_q = torch.tensor([0, 3, 5], dtype=torch.int32, device=device)
    cu_seqlens_k = torch.tensor([0, 3, 5], dtype=torch.int32, device=device)
    try:
        # TODO(student) ①: prefill — flash_attn_varlen_func contract (attention.py:67-70)
        prefill_out = fa_varlen(q, k, v, cu_seqlens_q=cu_seqlens_q, cu_seqlens_k=cu_seqlens_k,
                                max_seqlen_q=3, max_seqlen_k=3, softmax_scale=scale, causal=True)
        # decode cache (provided): paged (num_blocks, block_size, kv_heads, head_dim)
        q_dec = torch.stack([q[2], q[4]])                                  # last-token q per seq
        k_cache = torch.zeros(2, 4, num_kv_heads, head_dim, device=device, dtype=dtype)
        v_cache = torch.zeros_like(k_cache)
        k_cache[0, :3], k_cache[1, :2] = k[:3], k[3:5]                     # simple slice prefill
        v_cache[0, :3], v_cache[1, :2] = v[:3], v[3:5]
        block_table = torch.tensor([[0], [1]], dtype=torch.int32, device=device)
        cache_seqlens = torch.tensor([3, 2], dtype=torch.int32, device=device)
        # TODO(student) ②: decode — flash_attn_with_kvcache contract (attention.py:72-74)
        decode_out = fa_kvcache(q_dec.unsqueeze(1), k_cache, v_cache,
                                cache_seqlens=cache_seqlens, block_table=block_table,
                                softmax_scale=scale, causal=True).squeeze(1)
    except FAContractError:
        raise
    except Exception as e:
        raise FAContractError(
            f"flash_attn call failed — likely wrong arguments. "
            f"Mirror attention.py:67-70 (prefill) / 72-74 (decode).\nOriginal: {e}") from e
    return prefill_out, decode_out
```

- [ ] **步骤 4：运行测试，确认全过**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：`8/8 tests passed`，退出码 0。

- [ ] **步骤 5：提交**

```bash
git add course/lessons/lesson2/solution/lab_solved.py course/lessons/lesson2/solution/test_checks.py
git commit -m "feat(course/lesson2): 实现 simulate_fa_calls + _verify_fa_calls（mock UT + FAContractError 诊断）"
```

---

## Task 5：TDD `run_checks` + `main`/`main_cudagraph`/`_check_env`/`_print_trace`

**文件：**
- 修改：`course/lessons/lesson2/solution/lab_solved.py`（替换 `run_checks`/`_check_env`/`_print_trace`/`main`/`main_cudagraph` 桩）
- 测试：`course/lessons/lesson2/solution/test_checks.py`（追加 2 个 run_checks 测试）

**接口：**
- 产出：`run_checks(max_tokens, fa_results=None, block_size=BLOCK_SIZE) -> list[(name, ok)]`。从 `_trace` 取**第一条 prefill 记录**（含 `cu_seqlens_q`/`slot_mapping`）；从 `_seq_blocks` 取 `min(seq_id)`（首条 prompt = seq_A）的 `block_table`。断言：`Task1 captured prefill varlen call`（有 prefill 记录）、`Task1 captured decode kvcache call`（有 decode 记录）、`Task1 prefill q.shape is packed (total,H,D)`（prefill 记录 `q_shape[0]==cu_seqlens_q[-1]` 且 `len(q_shape)==3`）、`Task2 prefill_slot_mapping matches captured slot_mapping`（`prefill_slot_mapping(seq_A.block_table, block_size, 0, cu_seqlens_q[1]) == slot_mapping[:cu_seqlens_q[1]]`）、`Task4 prefill cu_seqlens_q == [0,a,a+b]`（`cu_seqlens_q` 单调、首项 0、末项=total）、`Task4 decode q.shape is (num_seqs,H,D)`（decode 记录 `len(q_shape)==3`、`q_shape[0]==len(context_lens)`）。若 `fa_results` 非 None（=(prefill_out, decode_out)），extend `_verify_fa_calls(prefill_out, decode_out)` 的三条。`main`/`_check_env`/`_print_trace`/`main_cudagraph` **不做单测**（需真实环境）—— 由 Task 9 验证。

- [ ] **步骤 1：追加 2 个失败测试（在 `TESTS =` 行之前）**

```python
def _build_trace_scenario():
    """Synthetic _trace + _seq_blocks for run_checks (block_size=4 for tiny numbers)."""
    lab._trace = [
        {"is_prefill": True,
         "q_shape": (6, 4, 64), "k_shape": (6, 1, 64), "v_shape": (6, 1, 64),
         "cu_seqlens_q": [0, 6, 9], "cu_seqlens_k": [0, 6, 9],
         "max_seqlen_q": 6, "max_seqlen_k": 6, "context_lens": None,
         "block_tables": None,
         "slot_mapping": [8, 9, 10, 11, 20, 21,   # seq_A (block_table [2,5]): 4 + 2
                          30, 31, 32]},            # seq_B (block 7): 3
        {"is_prefill": False,
         "q_shape": (2, 4, 64), "k_shape": (2, 1, 64), "v_shape": (2, 1, 64),
         "cu_seqlens_q": None, "cu_seqlens_k": None,
         "max_seqlen_q": 0, "max_seqlen_k": 0, "context_lens": [7, 4],
         "block_tables": (2, 2), "slot_mapping": [99, 100]},
    ]
    lab._seq_blocks = {10: [2, 5], 11: [7]}   # seq_A (id 10) = long prompt; seq_B (id 11)


def test_run_checks_all_pass_on_valid_scenario():
    _build_trace_scenario()
    failed = [n for n, ok in lab.run_checks(max_tokens=4, fa_results=None, block_size=4)
              if not ok and "Task3" not in n]
    assert not failed, f"unexpected failures: {failed}"


def test_run_checks_catches_wrong_slot_geometry():
    _build_trace_scenario()
    lab._trace[0]["slot_mapping"][0] = 999   # corrupt seq_A's first slot
    results = dict(lab.run_checks(max_tokens=4, fa_results=None, block_size=4))
    assert results["Task2 prefill_slot_mapping matches captured slot_mapping"] is False
```

- [ ] **步骤 2：运行测试，确认失败**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：两个新测试 FAIL（NotImplementedError），`8/10 tests passed`。

- [ ] **步骤 3：实现 `run_checks`**（替换桩）

```python
def run_checks(max_tokens, fa_results=None, block_size=BLOCK_SIZE):
    """Verify _trace + slot geometry (+ optional FA simulation) against invariants."""
    results = []
    prefill_recs = [r for r in _trace if r["is_prefill"]]
    decode_recs = [r for r in _trace if not r["is_prefill"]]
    results.append(("Task1 captured prefill varlen call", len(prefill_recs) >= 1))
    results.append(("Task1 captured decode kvcache call", len(decode_recs) >= 1))
    if prefill_recs:
        p = prefill_recs[0]
        q_shape = p["q_shape"]
        cu = p["cu_seqlens_q"]
        packed = len(q_shape) == 3 and cu is not None and q_shape[0] == cu[-1]
        results.append(("Task1 prefill q.shape is packed (total,H,D)", packed))
        results.append(("Task4 prefill cu_seqlens_q == [0,a,a+b]",
                        cu == sorted(cu) and cu[0] == 0 and len(cu) >= 2))
        # Task 2: reconstruct seq_A's slot slice and compare to captured
        if _seq_blocks:
            seq_a_id = min(_seq_blocks)
            a = cu[1]
            recon = prefill_slot_mapping(_seq_blocks[seq_a_id], block_size, 0, a)
            results.append(("Task2 prefill_slot_mapping matches captured slot_mapping",
                            recon == list(p["slot_mapping"])[:a]))
        else:
            results.append(("Task2 prefill_slot_mapping matches captured slot_mapping", False))
    if decode_recs:
        d = decode_recs[0]
        cl = d["context_lens"] or []
        results.append(("Task4 decode q.shape is (num_seqs,H,D)",
                        len(d["q_shape"]) == 3 and d["q_shape"][0] == len(cl)))
    if fa_results is not None:
        prefill_out, decode_out = fa_results
        results.extend(_verify_fa_calls(prefill_out, decode_out))
    return results
```

- [ ] **步骤 4：运行测试，确认全过**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：`10/10 tests passed`，退出码 0。

- [ ] **步骤 5：实现 `_check_env`/`_print_trace`/`main`/`main_cudagraph`**（替换桩，加在 `if __name__` 上方）

```python
def _check_env(model_path):
    missing = []
    try:
        import torch  # noqa: F401
    except Exception:
        missing.append("torch")
    try:
        import flash_attn  # noqa: F401
    except Exception:
        missing.append("flash-attn")
    if not os.path.isdir(model_path):
        missing.append(f"model at {model_path}")
    if missing:
        print("Missing prerequisites: " + ", ".join(missing))
        print("See course/lessons/lesson1/TUTORIAL.md §0 for setup.")
        raise SystemExit(1)


def _print_trace():
    print("\n--- trace (one row per engine step, layer 0's Attention.forward) ---")
    for i, r in enumerate(_trace):
        flag = "PREFILL" if r["is_prefill"] else "DECODE "
        if r["is_prefill"]:
            print(f"step {i:2d} {flag} q={r['q_shape']} cu_q={r['cu_seqlens_q']} "
                  f"cu_k={r['cu_seqlens_k']} slot[:6]={r['slot_mapping'][:6]}...")
        else:
            print(f"step {i:2d} {flag} q={r['q_shape']} ctx_lens={r['context_lens']} "
                  f"block_tables={r['block_tables']} slot={r['slot_mapping']}")


# a long English prompt that tokenizes to > 512 tokens (spans >= 3 blocks of 256)
_LONG_PROMPT = (
    "Explain in detail how paged attention works. " * 60
)


def main():
    from nanovllm import LLM, SamplingParams
    from nanovllm.engine.scheduler import Scheduler
    from nanovllm.layers.attention import Attention
    from nanovllm.utils.context import get_context
    from flash_attn import flash_attn_varlen_func, flash_attn_with_kvcache
    from transformers import AutoTokenizer
    import torch

    global _orig_attention_forward, _orig_postprocess, _get_context
    model_path = os.path.expanduser("~/huggingface/Qwen3-0.6B/")
    max_tokens = 4
    _check_env(model_path)

    # Task 3: manual FA call on real flash_attn (GPU). Raises FAContractError if mis-called.
    fa_prefill, fa_decode = simulate_fa_calls(
        flash_attn_varlen_func, flash_attn_with_kvcache, "cuda", torch.bfloat16)

    # install hooks — do NOT modify nanovllm source
    _orig_attention_forward = Attention.forward
    _orig_postprocess = Scheduler.postprocess
    _get_context = get_context
    Attention.forward = traced_attention_forward
    Scheduler.postprocess = traced_postprocess

    tokenizer = AutoTokenizer.from_pretrained(model_path)
    llm = LLM(model_path, enforce_eager=True, tensor_parallel_size=1)
    sp = SamplingParams(temperature=0.6, max_tokens=max_tokens)
    raw = [_LONG_PROMPT, "list all prime numbers within 100"]
    prompts = [tokenizer.apply_chat_template([{"role": "user", "content": p}],
                                             tokenize=False, add_generation_prompt=True)
               for p in raw]
    llm.generate(prompts, sp)

    _print_trace()
    print("\n--- checks ---")
    results = run_checks(max_tokens, fa_results=(fa_prefill, fa_decode))
    for name, ok in results:
        print(f"  [{'PASS' if ok else 'FAIL'}] {name}")
    if all(ok for _, ok in results):
        print("\nAll checks passed ✓")
    else:
        print("\nSome checks FAILED — review your TODO implementations.")
        raise SystemExit(1)


def main_cudagraph():
    """Run 2 (Q3b): observe the -1 sentinel in graph_vars['slot_mapping'].
    Separate process — a second LLM() in-process would re-init the NCCL group."""
    from nanovllm import LLM, SamplingParams
    from transformers import AutoTokenizer

    model_path = os.path.expanduser("~/huggingface/Qwen3-0.6B/")
    _check_env(model_path)
    try:
        llm = LLM(model_path, enforce_eager=False, tensor_parallel_size=1)
    except Exception as e:
        print(f"[Q3b] capture_cudagraph unavailable on this GPU: {e}")
        print("[Q3b] Fallback (explain-from-code): model_runner.py:206-207 does")
        print("       slot_mapping.fill_(-1) then overwrites [:bs]; attention.py:23 has")
        print("       `if slot == -1: return` — the -1 sentinel lets the CUDA-graph static")
        print("       buffer ignore slots beyond the real batch (else it would scatter into slot 0).")
        return
    tokenizer = AutoTokenizer.from_pretrained(model_path)
    sp = SamplingParams(temperature=0.6, max_tokens=4)
    raw = ["introduce yourself", "hi"]
    prompts = [tokenizer.apply_chat_template([{"role": "user", "content": p}],
                                             tokenize=False, add_generation_prompt=True)
               for p in raw]
    llm.generate(prompts, sp)

    sm = llm.model_runner.graph_vars["slot_mapping"]
    bs = 2
    n_sentinel = int((sm[bs:] == -1).sum().item())
    print("\n--- Q3b: cudagraph slot_mapping -1 sentinel (model_runner.py:206-207) ---")
    print(f"graph_vars['slot_mapping'].shape = {tuple(sm.shape)} (= min(max_num_seqs, 512))")
    print(f"  [:{bs}]  real slots : {sm[:bs].tolist()}")
    print(f"  [{bs}:]  -1 sentinel : {n_sentinel} of {sm.numel() - bs} are -1")
    print("  → store_kvcache_kernel skips them (attention.py:23 `if slot == -1: return`);")
    print("    without the sentinel, the static graph buffer would scatter stale KV into slot 0.")
```

- [ ] **步骤 6：确认合成测试仍通过（import 无副作用）**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：`10/10 tests passed`，退出码 0。（在没装 torch 的机器上，`import lab_solved` 不触发 nanovllm/flash_attn import；test 里用函数内 `import torch`，CPU 即可。）

- [ ] **步骤 7：提交**

```bash
git add course/lessons/lesson2/solution/lab_solved.py course/lessons/lesson2/solution/test_checks.py
git commit -m "feat(course/lesson2): 实现 run_checks + main(main_cudagraph) + 环境守卫"
```

---

## Task 6：派生学生版 `lab.py`（挖空三个 TODO）

**文件：**
- 创建：`course/lessons/lesson2/lab.py`（拷贝 `solution/lab_solved.py`，替换三个 TODO 体）

**接口：**
- 产出：`lab.py` 与 `lab_solved.py` 完全一致，唯独 `traced_attention_forward`、`prefill_slot_mapping`、`simulate_fa_calls` 的函数体换成带引导注释（含完整脚手架提示）的 `# TODO(student)` + `raise NotImplementedError(...)`。其余（`traced_postprocess`、`_verify_fa_calls`、`run_checks`、`main`、`main_cudagraph`、`_check_env`、`_print_trace`、`FAContractError`、TOY 常量、模块全局）保持一致、不动。

- [ ] **步骤 1：创建 `lab.py`**

创建 `course/lessons/lesson2/lab.py`，**内容与 `solution/lab_solved.py` 完全相同**，但把三个函数体分别替换为：

```python
def traced_attention_forward(self, q, k, v):
    """TODO(student) — Task 1 (trace): record this step's flash-attention call into
    _trace, then call _orig_attention_forward(self, q, k, v).

    Only LAYER 0 records (Attention.forward fires once per layer per step; every layer
    in a step shares context & shapes). Use:
        if _layer_order.setdefault(id(self), len(_layer_order)) == 0: ...
    Suggested record:
        {"is_prefill": context.is_prefill,
         "q_shape": tuple(q.shape), "k_shape": tuple(k.shape), "v_shape": tuple(v.shape),
         "cu_seqlens_q": context.cu_seqlens_q.tolist() if is_prefill else None,
         "cu_seqlens_k": context.cu_seqlens_k.tolist() if is_prefill else None,
         "max_seqlen_q": context.max_seqlen_q, "max_seqlen_k": context.max_seqlen_k,
         "context_lens": context.context_lens.tolist() if not is_prefill else None,
         "block_tables": (tuple(context.block_tables.shape) if context.block_tables is not None else None),
         "slot_mapping": context.slot_mapping.tolist()}
    Get the context via `context = _get_context()`. Mirror what Attention.forward reads
    at attention.py:59-75.
    """
    raise NotImplementedError("Task 1: fill in the FA-call trace recording")
```

```python
def prefill_slot_mapping(block_table, block_size, start, num_tokens):
    """TODO(student) — Task 2: reproduce model_runner.py:151-161 prefill slot scatter.

    Return each token's physical slot block_id*block_size + in-block offset. First block's
    start is further offset by start%block_size; last block is truncated to end - i*block_size.
    Return a list of length num_tokens.
    """
    raise NotImplementedError("Task 2: fill in prefill_slot_mapping")
```

```python
def simulate_fa_calls(fa_varlen, fa_kvcache, device, dtype):
    """TODO(student) — Task 3: call the two flash-attention interfaces on toy inputs.
    fa_varlen / fa_kvcache are passed in by main() (real flash_attn; mocks in tests) —
    you only CALL them. Focus on what args differ between prefill & decode.

    Toy inputs (2 seqs of lengths [3,2]; num_heads=4, num_kv_heads=1, head_dim=64):
        q = randn(5, num_heads, head_dim); k = v = randn(5, num_kv_heads, head_dim)
        cu_seqlens_q = cu_seqlens_k = [0,3,5];  scale = head_dim**-0.5
    ① prefill  — mirror attention.py:67-70:
        prefill_out = fa_varlen(q, k, v, cu_seqlens_q=…, cu_seqlens_k=…,
                                max_seqlen_q=3, max_seqlen_k=3, softmax_scale=scale, causal=True)
    ② decode cache (paged (2,4,kv_heads,head_dim)); q_dec = stack([q[2], q[4]]):
        k_cache[0,:3]=k[:3]; k_cache[1,:2]=k[3:5];  v_cache likewise
        block_table=[[0],[1]]; cache_seqlens=[3,2]
    ② decode   — mirror attention.py:72-74:
        decode_out = fa_kvcache(q_dec.unsqueeze(1), k_cache, v_cache,
                                cache_seqlens=…, block_table=…, softmax_scale=scale,
                                causal=True).squeeze(1)
    Wrap both calls in try/except: re-raise FAContractError; other Exception → raise
    FAContractError("...mirror attention.py:67-70/72-74...") from it. Return (prefill_out, decode_out).
    See TUTORIAL §3 worked example for the full call shape.
    """
    raise NotImplementedError("Task 3: fill in the two flash-attention calls")
```

`traced_postprocess`、`_verify_fa_calls`、`run_checks`、`main`、`main_cudagraph`、`_check_env`、`_print_trace`、`FAContractError`、TOY 常量与模块全局**保持与 `lab_solved.py` 一致，不动**。

- [ ] **步骤 2：确认 `lab.py` 能干净 import、且 TODO 在填前不可达**

运行：`python -c "import sys; sys.path.insert(0,'course/lessons/lesson2'); import lab; print('import ok')"`
预期：`import ok`（证明没有顶层 nanovllm/flash_attn import）。

- [ ] **步骤 3：提交**

```bash
git add course/lessons/lesson2/lab.py
git commit -m "feat(course/lesson2): 加学生版 lab.py（三个引导式 TODO）"
```

---

## Task 7：写 `TUTORIAL.md`

**文件：**
- 创建：`course/lessons/lesson2/TUTORIAL.md`

**接口：**
- 无代码接口。内容由 Task 10 的审核角色检查，不做单测。

- [ ] **步骤 1：写 `TUTORIAL.md`**，包含以下章节（完整正文 + 已核验锚点；每个 `file:line` 都要 grep 核对源码）：

  - **标题 + 一句话目标**：第 2 课——KV 缓存的接口与物理形态：从 flash-attention 调用到 paged 张量。
  - **受众前提**：懂 LLM 推理原理、会读 Python、不熟 vLLM/nano-vllm；**默认已学完 L1**。
  - **§0 环境初始化**：指向 `course/lessons/lesson1/TUTORIAL.md §0`（不重复）；点明 Q3b 额外需要 `enforce_eager=False`（cudagraph 可用）。
  - **§1 学完你能**：(a) 说清 prefill/decode 两个 FA 调用契约；(b) 亲手在 toy 输入上调两个 FA 接口；(c) 说清 paged 张量 + `num_kv_heads//world_size` + `num_kvcache_blocks` 反推；(d) 说清 `slot_mapping` 几何 + `store_kvcache` 哨兵 + cudagraph `-1` 哨兵；(e) 解释 `Context` 作调度↔计算解耦桥梁。
  - **§2 全景图**（**mermaid**，禁止中英混排 ASCII 框图）：prefill/decode 两条 FA 调用路径 + paged KV 张量 `(2,L,num_blocks,bs,kv_heads,D)` + **Context 解耦桥梁** + slot_mapping 散射示意。
  - **§3 逐段讲（锚 file:line）**：prefill varlen 调用 `attention.py:64-70`（含前缀缓存命中分支 `65-66`）；decode kvcache 调用 `attention.py:72-74`；paged 张量 + `num_kv_heads//world_size` + `block_bytes` + `num_kvcache_blocks` 反推 `model_runner.py:110/112/113/115`（点到每 rank 只存自己 kv heads）；`store_kvcache` kernel + `slot==-1` 哨兵 `attention.py:10-30/23` + 断言 `36-39`；slot_mapping 几何 `model_runner.py:151-161` + decode 单点 `181`；Context `context.py:5-27`；cudagraph `fill_(-1)` 哨兵 `model_runner.py:206-207`。**必含 worked example 子节**：用 toy 输入（2 seqs 长 [3,2]、GQA）完整示范两个 FA 签名的调用（逐参数 shape + prefill/decode 差异）——这是 Task 3 前置教学。
  - **§4 进 lab**：指向 `lab.py`，说明三个 TODO（Task1 `traced_attention_forward`、Task2 `prefill_slot_mapping`、Task3 `simulate_fa_calls`），Task4 是 observe+explain；运行 `.venv/bin/python lab.py` 全过打印 `All checks passed ✓`；Q3b 另跑 `.venv/bin/python lab.py --cudagraph`（注意：是学生版 lab.py 的 `--cudagraph`）。
  - **§5 图示补充**：prefill vs decode FA-args 对比表（**markdown 表格**）+ slot_mapping 三段偏移几何表 + `-1` 哨兵机制说明。

  讲义语气（ROLES ③）：预先验证、直接可跑的 step-by-step；**禁止**开发过程叙述。

- [ ] **步骤 2：提交**

```bash
git add course/lessons/lesson2/TUTORIAL.md
git commit -m "docs(course/lesson2): 写 TUTORIAL.md（FA 调用契约 + paged 形态 + worked example）"
```

---

## Task 8：写 `solution/ANSWERS.md`

**文件：**
- 创建：`course/lessons/lesson2/solution/ANSWERS.md`

- [ ] **步骤 1：写 `ANSWERS.md`**，分四部分：

  - **Task 1 参考实现**：贴 `lab_solved.py` 里 `traced_attention_forward` 完整代码；说明 layer-0-only 机制（`_layer_order`）；给出期望 trace 形态：step 0 PREFILL（`q_shape=(total,4,64)` packed、`cu_seqlens_q=[0,a,a+b]`、`slot_mapping` 长 a+b）、其后 DECODE（`q_shape=(2,4,64)`、`context_lens=[..,..]`、`slot_mapping` 两个单点）。
  - **Task 2 参考实现**：贴 `prefill_slot_mapping` 代码；解释三段偏移（首块 `+=start%block_size`、末块截断 `end-i*block_size`）；说明为何 live run 只覆盖 `start=0`、`start>0` 由单测覆盖（chunked prefill / 前缀缓存命中才会 `start>0`）。
  - **Task 3 参考实现**：贴 `simulate_fa_calls` 代码；逐行讲两个 FA 调用契约（prefill varlen vs decode kvcache 的参数差异）；解释值交叉验证为何成立（`q_dec` 取各 seq 末 token 的 q、cache 装同一批 KV ⇒ decode 输出 = prefill 末 token 输出）；说明 `FAContractError` 诊断（漏 unsqueeze / cu_seqlens 错 / cache_seqlens-block_table 不对齐 各自的提示）。
  - **Task 4 explain 参考答案**（rubric）：① 为何 varlen packed（无 padding、省算力）而非 padded batched；② decode 为何 `unsqueeze(1)`（注入 seq_len=1 query 维）而 prefill 不需要、`cache_seqlens`（每 seq 有效 KV 数）vs `block_table`（seq→物理块映射）职责；③ prefill vs decode 根本差异（prefill 多 query + KV 本步新算、decode 单 query + KV 全来自缓存）；④ `-1` 哨兵为何必要（cudagraph 静态缓冲固定大小、超出真实 batch 的槽必须 no-op，否则 store_kvcache 会散射进 slot 0 污染）、`model_runner.py:206-207` + `attention.py:23`。每题给判分要点。TODO→答案映射：`lab.py` 三个 TODO 直接对应 `lab_solved.py` 三个函数体。

- [ ] **步骤 2：提交**

```bash
git add course/lessons/lesson2/solution/ANSWERS.md
git commit -m "docs(course/lesson2): 写 solution/ANSWERS.md（参考答案 + 判分要点）"
```

---

## Task 9：参考答案的端到端运行（依赖环境）

**文件：**
- 不改任何文件。在真实 GPU 上验证 `solution/lab_solved.py`。

**接口：**
- 消费：一个可用的 nano-vllm 环境（`.venv`：torch 2.8.0+cu128 + flash-attn 2.8.3 + Qwen3-0.6B + 一张 CUDA 卡）。

> **此任务依赖环境。** 本服务器 base 环境缺 torch/triton/flash-attn 与模型（见 `CLAUDE.md` 环境）。`.venv` 据称已就绪；若实际跑失败（通常是 flash-attn / cudagraph），**停下并把阻塞反馈给用户**——不要标记完成，也不要伪造输出。

- [ ] **步骤 1：确认环境**

运行：`ls .venv/bin/python && .venv/bin/python -c "import torch,flash_attn; print('torch',torch.__version__,'fa',flash_attn.__version__)"` 以及 `ls ~/huggingface/Qwen3-0.6B/`
预期：`.venv/bin/python` 存在；打印 torch / flash-attn 版本；模型目录存在。缺则按 `CLAUDE.md`/L01 §0 装好再继续；装不上则反馈用户。

- [ ] **步骤 2：跑 GPU-free 合成单测（应已过，再确认一次）**

运行：`python course/lessons/lesson2/solution/test_checks.py`
预期：`10/10 tests passed`，退出码 0。

- [ ] **步骤 3：Run 1 端到端（enforce_eager=True）**

运行：`.venv/bin/python course/lessons/lesson2/solution/lab_solved.py`
预期：打印 `--- trace ---`、`--- checks ---`（Task1/2/3/4 全 `PASS`），最后 `All checks passed ✓`，退出码 0。Task3 的三条（含值交叉验证 `allclose`）必须在真实 FA 上通过。

- [ ] **步骤 4：Run 2 cudagraph 哨兵观察**

运行：`.venv/bin/python course/lessons/lesson2/solution/lab_solved.py --cudagraph`
预期：打印 `--- Q3b: cudagraph slot_mapping -1 sentinel ---`，`[bs:]` 段有大量 `-1`（`(sm[bs:]==-1).sum()` ≈ `sm.numel()-bs`）。若 `capture_cudagraph` 失败，走 fallback 说明文（不视为阻塞）。

- [ ] **步骤 5：跑学生版 `lab.py`，确认未填时 fail-closed**

运行：`.venv/bin/python course/lessons/lesson2/lab.py`
预期：在第一个 TODO（`traced_attention_forward`）处抛 `NotImplementedError` 非零退出（证明学生必须先填）。步骤 3 已证明填好后能过。

- [ ] **步骤 6：提交**（仅当环境工作改动了 tracked 文件；否则不提交）

```bash
# 仅当需要：
git add -A && git commit -m "chore(course/lesson2): 端到端运行所需的环境修复"
```

---

## Task 10：审核 + 学生试课验收（三角色 subagent 闸门，D2=A）

**文件：**
- 除非审核/试课暴露需要改的地方，否则不改文件。

**接口：**
- 消费：Task 1–9 的全部产物。派角色 prompt **从 `course/ROLES.md` 取**，按八维验收。

这是 spec §9 的三角色闸门（D2=A：**真用 subagent 跑**）。

- [ ] **步骤 1：派 reviewer subagent** —— 用 `Agent` 工具（subagent_type 用 `general-purpose` 或 `Explore`+审核 prompt），prompt 取 ROLES.md 的"课程架构审核工程师"角色 + 八维 checklist，对照 nano-vllm 源码 + 本 spec，**逐维**审计 `TUTORIAL.md`/`lab.py`/`solution/`，输出优先级化问题清单（阻塞/重要/次要，每条带 `file:line` + 改法）。重点：⑤ 每个 `file:line` 逐条 grep（含 lab.py 内部行号漂移）+ ⑧ 锚点 + ③ 讲义语气（无开发叙述）+ ② 图（mermaid/markdown，无中英混排 ASCII）+ ④ 讲义↔lab 1:1。

- [ ] **步骤 2：派 student subagent 盲测** —— 用 `Agent` 工具，prompt 取 ROLES.md 的"学生"角色（persona：算子开发者、懂推理原理、会读 Python、不熟 vLLM），**只给它 `TUTORIAL.md` + `lab.py`**（不许看 `solution/`）。让它真做一遍：读 §0–§5、填三个 TODO、跑 `.venv/bin/python lab.py`、跑 `--cudagraph`。记录卡点（概念跳步 / 接口不清 / 验证不足 / 错误提示不友好 / FA 调用不会）；必须亲手跑出 `All checks passed ✓`。

- [ ] **步骤 3：迭代修改** —— 把 reviewer + student 问题反馈给工程师；改；再审/再试。重复到 reviewer **无阻塞/重要项**且 student 试课**无阻塞性卡点**。改 lab.py/solution 后**重核 lab.py 内部行号锚点**（L01 漂过 ~30 行）。

- [ ] **步骤 4：提交修改并标记完成**

```bash
git add -A && git commit -m "docs(course/lesson2): 应用审核 + 学生试课反馈"
```

**整份计划验收**：Task 1–8 完成；Task 9 在真实环境 Run 1 打印 `All checks passed ✓` + Run 2 展示 `-1` 哨兵（或 fallback）；Task 10 reviewer 无阻塞/重要 + student 盲测无阻塞。
